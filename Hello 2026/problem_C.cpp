#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <string>
using namespace std;

void solve() {    
    int n, m, k;
    cin >> n;
    cin >> m; 
    cin >> k;

    // there is a relation with the 1 + 2 + 3 + 4
    // basically, if you want to move a 
    
}

int main() {
    int t;
    cin >> t; 
    while(t--) {
        solve();
    }
    return 0;
}